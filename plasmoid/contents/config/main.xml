<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
      http://www.kde.org/standards/kcfg/1.0/kcfg.xsd">
  <kcfgfile arg="true"/>
  <group name="General">
    <!-- 0 means "not configured yet". The config UI queries nvidia-smi to
         suggest real values the first time the user opens settings. -->
    <entry name="defaultWatts" type="Int">
      <default>0</default>
      <label>Power limit in watts used for the OFF state.</label>
    </entry>
    <entry name="boostWatts" type="Int">
      <default>0</default>
      <label>Power limit in watts used for the BOOST state.</label>
    </entry>
    <entry name="debugLogging" type="Bool">
      <default>false</default>
      <label>Print debug messages to the console.</label>
    </entry>
  </group>
  <group name="Advanced">
    <!-- Both of these default to false so existing installs keep their
         current behavior after an update; the Advanced config page is
         where the user opts in. -->
    <entry name="serviceIdlingEnabled" type="Bool">
      <default>false</default>
      <label>Pause background services while boosted.</label>
    </entry>
    <entry name="idleBaloo" type="Bool">
      <default>true</default>
      <label>Suspend the Baloo file indexer while boosted.</label>
    </entry>
    <entry name="idleAkonadi" type="Bool">
      <default>true</default>
      <label>Stop Akonadi (PIM/mail sync) while boosted.</label>
    </entry>
    <entry name="idleKalarm" type="Bool">
      <default>false</default>
      <label>Stop the KAlarm daemon while boosted.</label>
    </entry>
    <entry name="customIdleUnits" type="String">
      <default></default>
      <label>Extra systemctl --user unit names to stop while boosted, comma or newline separated.</label>
    </entry>
    <entry name="powerProfileEnabled" type="Bool">
      <default>false</default>
      <label>Switch to the performance power profile while boosted.</label>
    </entry>
    <entry name="chromeIdlingEnabled" type="Bool">
      <default>false</default>
      <label>Deprioritize matching browser processes while boosted.</label>
    </entry>
    <entry name="chromeProcessPattern" type="String">
      <default>chrome|chromium</default>
      <label>Extended-regex pattern (grep -E) matched against process command lines to find browser processes to deprioritize.</label>
    </entry>
    <entry name="chromeNiceValue" type="Int">
      <default>15</default>
      <label>Nice value (0-19) applied to matching browser processes while boosted.</label>
    </entry>
  </group>
</kcfg>
